services:
  arangodb:
    # ArangoDB server 3.12; the application uses the Go driver v2.
    image: arangodb/arangodb:3.12
    container_name: dota-leagues-arangodb
    ports:
      - "127.0.0.1:8529:8529"
    environment:
      ARANGO_NO_AUTH: "1"
