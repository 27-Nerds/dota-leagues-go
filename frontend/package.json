{
  "name": "dota-leagues-front",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "check": "svelte-check --fail-on-warnings",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/deploy.js",
    "test": "node --test"
  },
  "devDependencies": {
    "svelte": "5.57.0",
    "vite": "8.2.2",
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte-check": "4.4.8",
    "typescript": "^5.9.3"
  }
}
